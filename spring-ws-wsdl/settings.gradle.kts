enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")

rootProject.name = "spring-ws-wsdl"

include(":wsdl")
include(":client")
include(":server")

pluginManagement {
    plugins {
    }
    resolutionStrategy {

    }
}
